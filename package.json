{
  "name": "edusetu-monorepo",
  "version": "1.0.0",
  "description": "Unified runner for EduSetu Frontend and Backend",
  "main": "index.js",
  "scripts": {
    "dev": "(npm run dev --prefix frontend &) && (cd backend && source ../.venv/bin/activate && python3 app.py)",
    "stop": "pkill -f 'python3 app.py' && pkill -f 'vite'",
    "install-all": "npm install --prefix frontend && pip install -r backend/requirements.txt"
  },
  "devDependencies": {
  }

}
